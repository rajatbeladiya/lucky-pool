import React, { Fragment } from 'react';

import CreatePoolDialog from './modules/landing/components/CreatePool/CreatePoolDialog';

const Dialogs = () => (
    <Fragment>
        <CreatePoolDialog />
    </Fragment>
);

export default Dialogs;
